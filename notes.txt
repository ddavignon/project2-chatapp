# use virtual environment to install psycopg2

pip install flask-sqlalchemy psycopg2

# if error is given

sudo apt-get install libpq-dev
sudo apt-get install python-dev
pip install psycopg2

# sudo apt install postgresql-server-dev-all    ? not sure if needed

# pip install --upgrade wheel                   ? found in forum, didn't try